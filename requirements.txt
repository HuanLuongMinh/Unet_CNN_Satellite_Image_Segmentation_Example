# torch==2.10.0+cu128 và torchvision==0.25.0+cu128 đã được Kaggle pre-install sẵn.
# Không cài lại để tránh upgrade numba/cuda-core làm vỡ cudf, cuml, dask-cuda.
timm==1.0.26
albumentations>=1.3.1
einops>=0.7.0
rasterio>=1.4

# ── Ghim lại đúng dải version mà cudf/cuml/dask-cuda đã pre-install trên Kaggle yêu cầu.
# albumentations/rasterio kéo theo numba/numba-cuda/cuda-core mới hơn nếu không ghim,
# làm pip nâng cấp và vỡ tương thích với stack RAPIDS có sẵn trên Kaggle.
numba>=0.60.0,<0.62.0
numba-cuda>=0.22.2,<0.23.0
cuda-core==0.3.*
